alias ac="sudo apt-get -qq autoclean && sudo apt-get -qq autoremove"
alias au="sudo apt-get -qq update"
alias ba="vim ~/.bash_aliases"
alias br="vim ~/.bashrc"
alias c="clear"
alias dr="sudo shutdown -r now"
alias g="cd /var/www/ghost"
alias gr="service ghost restart"
alias gs="service ghost status"
alias h="cd ~/"
alias ns="service nginx status"
alias nr="service nginx restart"
alias r=". ~/.bashrc"
alias re="nr && gr && dr"
alias su="sudo apt-get -qq update && sudo apt-get -qq upgrade && sudo apt-get -qq update && sudo apt-get -qq dist-upgrade && sudo apt-get -qq update && do-release-upgrade -qq && sudo apt-get -qq update && sudo apt-get -qq autoclean && sudo apt-get -qq autoremove && sudo apt-get update"
alias x="exit"
alias vp="vim +PluginInstall +qall && vim +PluginClean +qall && vim +PluginUpdate +qall"
alias vr="vim ~/.vimrc"
